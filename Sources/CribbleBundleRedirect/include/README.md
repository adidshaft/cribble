# include

Public C header for the [`CribbleBundleRedirect`](..) target.

`CribbleBundleRedirect.h` declares the symbols imported by the Swift app target
via the module map, so Swift can call into the bundle-redirect shim.
